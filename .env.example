# 伺服器設定
HOST=0.0.0.0
PORT=8000
DEBUG=false

# CORS 設定 (多個來源以逗號分隔)
# CORS_ORIGINS=["http://localhost:3000", "https://example.com"]

# 上傳限制
MAX_UPLOAD_SIZE_MB=10.0

# 圖片處理預設值
DEFAULT_TARGET_SIZE_MB=2.0
DEFAULT_MIN_SIZE_MB=0.0
DEFAULT_TARGET_RATIO=1.7777
